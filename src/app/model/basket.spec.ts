/*import { Basket } from './basket';
import { Product } from './product';

describe('Basket', () => {
  it('should create an instance', () => {
    expect(new Basket(0,0,0,0,new Product()[], false)).toBeTruthy();
  });
});

*/