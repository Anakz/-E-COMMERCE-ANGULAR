/*import { Product } from './product';

describe('Product', () => {
  it('should create an instance', () => {
    expect(new Product(name:String, description:String, buying_price:Float32Array, selling_price:Float32Array, stock:Uint8Array, stock_available:Uint8Array, weight:Uint8Array, id_category:Uint8Array, basket:Object[], order:Object[], images:Object[]):Product).toBeTruthy();
  });
});
*/